from setuptools import setup, find_packages

1;95;0csetup(
    name="daylily",
    version="0.7.57",
    packages=find_packages(),
    install_requires=[
        # Add dependencies here,
        # 'pytest',
    ],
    entry_points={
        "console_scripts": [
        ],
    },
)
 
 
