from setuptools import setup, find_packages

setup(
    name="daylily",
    version="0.6.17",
    packages=find_packages(),
    install_requires=[
        # Add dependencies here,
        # 'pytest',
    ],
    entry_points={
        "console_scripts": [
        ],
    },
)
